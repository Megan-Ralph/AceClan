<section class="content">
  <div class="container">
    <div class="col-md-8 offset-md-2">
      <h4 class="comment-title"><%= @game.name %></h4>
      <p><%= @game.description %></p>

      <%= render "regiment" %>
    </div>
  </div>
</section>
