<h5 class="comment-title">REGIMENT MEMBERS</h5>

<% if @users.any? %>
  <% @users.each do |user| %>
    <p><%= user.username %> - <%= user.forum_primary_group_name %></p>
  <% end %>
<% else %>
  <p>There are no other users in your regiment!</p>
<% end %>
